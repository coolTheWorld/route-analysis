# 0.2.0 发布核对

日期：2026-08-14
目标：Windows x64 文件夹发布物

## 质量门禁

- [x] 单元、几何、存储、日志和 Qt 组件/主流程测试通过：88 项。
- [x] Ruff 全项目检查通过。
- [x] mypy 全项目检查通过。
- [ ] 源码 `--smoke-test` 通过。
- [ ] `pip-audit` 运行依赖与完整开发环境未发现已知漏洞。
- [ ] PyInstaller Windows x64 文件夹构建通过，无新增阻断警告。
- [ ] `dist/RouteAnalysis/RouteAnalysis.exe --smoke-test` 通过。
- [ ] Qt 实际渲染截图完成目视检查。
- [ ] 发布目录不包含 `data/`、`log/`、配置、密码、令牌或构建缓存。

## 五轴代码审查

### 正确性

- [x] 尖角、真实圆弧、分段贝塞尔、开放/闭合、偏差边界、失败回退和单次撤销有回归测试。
- [x] 圆弧半径编辑保持理论交点和两侧方向，超过最大允许值时拒绝。
- [x] schema 2 圆弧读写和 schema 1 兼容读取有测试。
- [x] 转弯严格阈值、`-π/π` 回绕、反向抵消/切换、严格掉头阈值、缺 yaw 分段有测试。
- [x] 距离窗口 ICR、左右/倒车、四角统计、跳过样本和全局最大位置有测试。
- [x] 日志级别、UTF-8 JSON、每日/20 MiB 轮转、30 文件保留、午夜空闲边界和故障恢复有测试。
- [x] 设置、预览、半径结果树、画布定位、日志状态和主窗口后台路径流程有 Qt 测试。

### 可读性与架构

- [x] 自动车道、四角半径和日志为独立纯逻辑/基础设施模块；Qt 只编排与呈现。
- [x] 自动生成对话框封装自己的预览生命周期，主窗口只持久化确认结果。
- [x] 日志菜单从主窗口抽离；`main_window.py` 保持在 1000 行以内。
- [x] 无 TODO/FIXME、调试打印、无用代码或未说明的静默兼容分支。
- [x] 未新增第三方依赖，继续复用标准库、PySide6 和 Shapely。

### 安全与已接受风险

- [x] 业务客户端除登录 POST 外仍只实现约定 GET，不新增后端写操作。
- [x] URL、响应、路径数字、配置、车道 schema 与导入目标路径继续在边界校验。
- [x] 未在源码、测试或发布物中写入真实密码、令牌或业务路径。
- [x] 用户明确要求 DEBUG 未脱敏记录密码、令牌、Authorization/tenant 请求头、配置、响应、路径和导入正文；ADR 0004、README、用户手册和威胁模型一致说明。
- [x] 用户明确不要求日志 ACL、加密、脱敏、上传/发送/清空或其他应用级安全控制。

### 性能

- [x] 订单和任务保持服务端分页；后台 HTTP 与分析不阻塞 Qt 主线程。
- [x] 通行分析和四角半径共用单个后台任务；代次不匹配的迟到结果被丢弃。
- [x] 圆弧拟合先按偏差简化并限制单窗口样本数；400 点半圆由约 6163 ms 降至约 8 ms。
- [x] 600 点密集圆弧和 1200 点高频折线有 1.5 秒性能预算回归。
- [x] 画布车辆、异常和半径图层可独立关闭；半径图层默认关闭。

## 需求追踪

- [x] 从下发/实际路径仅新增普通车道，默认下发，支持预览、临时宽度、近闭合和三种弯道模式。
- [x] 真实圆弧与边界圆角语义分离，支持自动拟合、失败计数、回退和半径编辑。
- [x] 每个弯道显示四角最大值及位置，展开显示最小/中位/最大；整条路径有四类全局最大。
- [x] 默认转弯阈值 `π/6 rad`、掉头阈值 `0.75π rad`、半径窗口 `0.5 m`，均按确认规则计算。
- [x] VIN 尺寸优先于全局尺寸并在结果区注明来源。
- [x] `log/` 与 `data/` 同级，默认 INFO，等级立即生效并持久化，帮助菜单可打开日志。
- [x] 新增参数兼容旧配置，车道 schema 1 读取、schema 2 保存/导出。
- [x] 所有代码和文档仅写入 `route-analysis/`。

## 发布与回退

这是便携桌面文件夹发布，不修改服务端、不需要数据库迁移，也没有远程功能开关。建议把 0.2.0 复制到独立目录，以只读调度账号先验证一个已知订单/命令，再替换操作者版本。

回退步骤：

1. 关闭应用，完整备份当前 `data/` 与 `log/`。
2. 恢复上一版本的完整 `RouteAnalysis/` 程序文件夹。
3. `config.json` 的新增字段可由 0.1.0 忽略；不要让 0.1.0 打开 schema 2 车道文件。
4. 对从 schema 1 升级过的地图，把对应 `.json.bak` 恢复为 `.json` 后再使用 0.1.0。
5. 0.2.0 新建且含真实圆弧的布局没有可逆 schema 1 表达；应保留该数据并继续使用 0.2.0，或人工重建为直线/贝塞尔后再回退。

发布后首小时人工检查：应用启动、租户登录、订单分页、两类路径点数、自动车道预览、四角半径结果、保存/重启、INFO 日志流和“日志不可用”状态。若出现数据完整性问题、持续未捕获异常或关键流程不可用，立即回退程序目录并恢复车道备份。
