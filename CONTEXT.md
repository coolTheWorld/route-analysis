# 路线分析

路线分析用于回看调度订单中的命令路线，并将路线坐标还原为叉车在地图上的车体占用范围。

## Language

**订单**:
调度系统中的顶层业务记录，包含一个或多个按执行过程产生的任务。

**任务**:
订单中的一次运行任务，包含按执行过程产生的一个或多个命令。

**命令**:
任务中面向车辆的一次执行单元，是查看下发路径和实际执行路径的最小上下文。

**下发路径**:
命令下发给车辆的预期位姿点序列。
_Avoid_: 系统路径

**实际执行路径**:
车辆执行命令期间由定位心跳形成的实际位姿点序列。
_Avoid_: 实际路径

**前轴中心点**:
后端路径坐标所代表的车辆参考点，位于叉车前轴的横向中心。

**车辆包络**:
以米为单位，由车宽、前轴中心到车头的正向距离、前轴中心到车尾的反向距离定义，并按位姿点航向旋转的车辆矩形范围。三项尺寸均须大于零，车长等于前距与后距之和。
_Avoid_: 点、圆点

**车辆扫掠范围**:
车辆沿相邻路径点之间的连续插值位姿运动时，所有车辆包络共同覆盖的区域。越界分析使用车辆扫掠范围，避免只检查离散路径点造成漏判。
_Avoid_: 路径折线

**转弯区间**:
车辆沿同一方向持续改变航向的一段连续路径，方向按整弯等效旋转中心位于入弯车体航向的左侧或右侧定义，不因前进或倒车而翻转。转弯半径结果以区间为单位展示。
_Avoid_: 单个转弯点

**转弯端点**:
界定一次转弯半径测量的入弯点与出弯点对。端点身份由其在来源路径中的样本序号确定，因此坐标相同但序号不同的样本属于不同端点。转弯端点由用户从同一条路径已有的坐标点中按先后顺序选择；系统不建议端点。
_Avoid_: 半径采样点、局部窗口端点

**转弯半径测量**:
在一条路径上由一对转弯端点界定的独立整弯半径结果。同一条路径可以拥有多次转弯半径测量；结果仅保存在本地，直到用户删除或源路径发生变化。
_Avoid_: 局部半径统计、分段半径样本

**手动半径测量**:
用户从同一条路径中依次选择入弯点和出弯点后建立的转弯半径测量。这是唯一一种转弯半径测量，允许用户改名和删除。

**端点候选**:
在画布上点击一次后供用户挑选转弯端点的样本集合，由离点击位置最近的样本及其在同一条路径上前后各两个可绘制样本，与本次点击命中的其余样本合并去重后按路径顺序排列。路径首尾附近实际存在的邻居不足时集合相应变短，不向另一侧补齐。端点候选只在选择过程中列出供人辨认，本身不参与半径计算。
_Avoid_: 半径采样窗口、局部窗口

**测量名称**:
用户识别一条转弯半径测量的可编辑非空文本。系统提供带数字后缀的默认名称，但删除其他测量不会使已有名称重新编号。
_Avoid_: 固定编号、结果序号

**掉头区间**:
累计绝对 yaw 变化严格大于 `0.75π rad` 的转弯区间，方向仍按整弯等效旋转中心相对入弯车体航向的左右侧定义。掉头使用与普通转弯相同的四角半径计算。

**整弯等效旋转中心**:
由一个转弯区间的入弯位置、出弯位置及两端之间连续展开的累计 yaw 旋转量确定的固定圆心。中间样本只用于消除角度环绕歧义，不形成局部半径样本；一个转弯区间只有一个整弯等效旋转中心。
_Avoid_: 瞬时旋转中心、局部旋转中心

**前轴中心转弯半径**:
整弯等效旋转中心到入弯前轴中心点的距离。它与车辆宽度、中心前距及中心后距无关，是每条转弯半径测量中的固定第五项半径。
_Avoid_: 车辆坐标半径、中心点半径

**四角转弯半径**:
整弯等效旋转中心到入弯车辆前外角、后外角、前内角和后内角的距离；左转与右转时内外侧自动交换。每个转弯区间只产生四个固定半径。
_Avoid_: 单一车辆转弯半径、前距外切、后距外切、前距内切、后距内切

**地图方向**:
整张路线分析图的全局显示旋转量，单位为弧度；默认 `0 rad` 表示东向朝屏幕右侧。它同步作用于路径、车辆包络和车道，但不改变后端原始坐标或航向。
_Avoid_: 角度、坐标修正

**车道**:
用户在地图上绘制的车辆通行空间；每条车道拥有独立的总宽度，表示从一侧边界到另一侧边界的距离。车道用于把车辆包络与道路空间叠加比较，观察车辆转弯时能否通过。
_Avoid_: 车辆侧边扩展区、安全余量

**车道布局**:
归属于一张地图、可在该地图的不同订单与命令之间复用的一组车道。

**通行区域**:
当前地图全部启用车道区域的并集。车辆包络完全位于该并集内时视为未越界，路径不需要绑定到某一条车道。

**车道走向线**:
由多个锚点及其间的直线、圆弧或三次贝塞尔线段形成的车道中心线。走向线可为带平直端面的开放线，也可首尾连接为没有端面的闭合环路；车道以该线为中心向左右各扩展总宽度的一半。
_Avoid_: 车辆路径、下发路径

**车道中心线长度**:
车道走向线全部直线、圆弧和贝塞尔线段的弧长之和；闭合车道包含首尾闭合线段。车道总宽和开放车道端部不计入该长度。
_Avoid_: 车道区域周长、边界长度、首尾直线距离

**车道几何中心**:
完整车道走向线实际边界框的中心，边界包含直线、圆弧和贝塞尔曲线的范围。车道长度缩放以该点为不动点。
_Avoid_: 锚点平均值、中心线重心、车道区域质心

**车道长度缩放**:
以车道几何中心为不动点，按同一比例改变整条走向线几何，使车道中心线长度达到目标值。缩放保持车道形状比例，不改变车道总宽。
_Avoid_: 延长末段、改变车道宽度

**车道平移**:
保持车道走向线形状、中心线长度和总宽不变，将全部锚点、圆弧圆心及贝塞尔控制点移动相同距离的操作。一次连续鼠标拖动视为一次车道平移。
_Avoid_: 拖动锚点、画布平移

**自动生成车道**:
以当前路径上用户选定的两个点位为范围生成的普通车道。生成完成后车道与来源路径解除动态绑定，可按普通车道继续编辑、保存和复用。系统不提供整条路径一次生成。
_Avoid_: 路径绑定车道、同步车道、整条路径生成

**两点连接方式**:
把选定的两个点位构造成车道走向线的方式，可为保留两点之间实际形状的沿原路径，或忽略中间形状的直线连接。直线连接不经过弯道拟合，弯道生成模式、最大拟合偏差和闭合车道对它没有作用。
_Avoid_: 生成范围、路径截取

**车体外伸量**:
一个点位的车辆包络沿该点位处车道走向线方向探出该点位的最远距离。自动生成车道的两端各按其外伸量延伸，使两个点位的车辆包络在长度方向上都被车道覆盖。走向线方向与位姿航向一致时它等于中心前距或中心后距，不一致时仍按包络四角的投影取最大值。
_Avoid_: 前后距延长、端部余量

**车辆分段显示**:
画布绘制车辆包络时可选择整车、仅前段或仅后段。分界落在前轴中心点上，因此仅前段画出的正是中心前距那一段、仅后段画出的正是中心后距那一段。这只是显示选择，越界判定与最小净距永远使用完整车辆包络。场景速算的扫掠包络沿用同一套分段，并给两段各自固定的颜色，颜色对应关系写在图例里；那里的三个选项直接叫整车、中心前距、中心后距，因为它们就排在这两项尺寸的输入框下面。
_Avoid_: 半车分析、按前距判定

**模拟运行**:
场景速算俯视图上让一个车体包络自起点沿路径走到终点的一次回放。它只重画已经算好的位姿，不重新求解，也不改变任何尺寸；双向布局的两个机动同时走。
_Avoid_: 仿真、动力学回放

**弯道生成模式**:
自动生成车道时用于把来源路径转弯构造成中心线的方式，可为保留原始夹角的尖角、与前后直线相切的真实圆弧或三次贝塞尔曲线。圆弧模式改变中心线本身，不等同于仅把车道边界倒圆。
_Avoid_: 边界连接样式

**最大拟合偏差**:
自动生成车道的中心线与来源路径坐标之间允许的最大距离，单位为米；默认值为 `0.05 m`。该值统一约束坐标简化、圆弧拟合和贝塞尔拟合。
_Avoid_: 采样步长、车道余量

**车道连接样式**:
相邻车道段在中间锚点处形成边界的方式，可为圆角或带尖角长度限制的尖角连接。每条车道拥有默认样式且锚点可以覆盖；新建车道默认使用尖角连接。尖角保留来源路径的实际转向夹角，不把转弯强制改成 90°。
_Avoid_: 直角连接

**越界点**:
车辆包络未被已绘制车道区域完全包含的路径点。越界标记是路线分析的可视化辅助结论，不代表正式安全认证。
_Avoid_: 碰撞点

**最小净距**:
车辆包络完全位于通行区域内时，车辆包络与通行区域边界之间的最短距离。净距低于可配置阈值时标记为临界，默认阈值为 `0.15 m`。

**通行余量**:
针对一条下发路径，把逐处最小净距、可行的路径横向偏置范围和最紧的几处瓶颈放在同一条横轴上给出的分析结果。通行余量只给建议，不修改下发路径，也不修改车道。
_Avoid_: 余量分析、间隙分析

**净距剖面**:
沿下发路径按分析步长逐样本给出的最小净距序列，横轴为路径进度百分比。
_Avoid_: 净距曲线、间隙曲线

**区段**:
通行余量分析把一条下发路径切分成的直行段与转弯段。转弯段由转弯区间识别得到并收紧到实际改变航向的样本；该切分只服务于通行余量视图，不产生转弯半径测量、不写入本地测量文件、不在画布上标注转弯端点。区段以点位序号区间加所覆盖车道名标识。
_Avoid_: 路径分段、自动转弯识别

**路径偏置**:
把一个区段的全部位姿沿行进方向的法向整体平移的量，正值指向行进方向左侧。偏置只用于求解和建议，不写回下发路径。转角语境下同一个量按“向内、向外”表述，避免正负号在左转与右转之间翻转含义。
_Avoid_: 路径平移、车道偏移

**可行偏置带**:
使一个区段的最小净距不低于净距阈值的全部偏置取值区间。区间为空表示该段在任何偏置下都无法通过。
_Avoid_: 偏置建议范围

**瓶颈**:
一个区段内最小净距最低的位姿，连同该处的约束部位、回到可行偏置带所需的最小偏置和路径进度。约束部位区分四角与车身侧边，因为约束通常来自倒车侧后外角外摆而不是车宽。
_Avoid_: 危险点、最险处

**需求道宽**:
使一个区段的最小净距恰好等于净距阈值所需的覆盖车道总宽。分“居中即可通过”和“需路径偏置”两个下界，由改写该车道总宽、重建通行区域后重跑既有判定链路二分求得，不使用闭式公式。
_Avoid_: 最小通道宽度、理论道宽

**耦合冲突**:
相邻两个转弯之间的连接段只有一个偏置值，而前后两个转弯各自要求的可行偏置带没有交集的情况。系统检测并报告冲突，不求所需的半径调整量。
_Avoid_: 偏置矛盾

**车道倒角半径**:
车道走向线在一个转角处圆弧段的半径。它与该处下发路径的转弯半径是两个独立的量；两者不等时，两条中心线的横向间距沿弯从零变到峰值再回到零，用单个常数偏置近似会恰好在弯心处高估余量。
_Avoid_: 车道转弯半径

**拟合转角**:
把一个转弯区段还原成直线—圆弧—直线参数化形式的结果，用于转角求解。拟合与原始离散路径存在残差，该残差随结果一并显示，不隐藏。
_Avoid_: 理想转角

**起弯点**:
转角求解的第三个自由度，即圆弧起点沿入弯直线的纵向位移，正值表示推迟起弯。入弯段与出弯段的偏置一旦定下，切于两条腿且转角固定的圆弧只剩一个参数族，起弯点选的正是族中的哪一条，因此推迟起弯等价于收紧路径转弯半径。
_Avoid_: 切点偏移、起弯延迟量

**场景速算**:
不连接任何数据源，仅凭车辆参数与道路尺寸对典型路口和掉头工况做的离线几何计算。它与通行余量互不共享路径：通行余量分析一条真实的下发路径，场景速算的位姿序列完全由参数生成。
_Avoid_: 理论计算、离线分析

**工况**:
车辆是否被允许偏离道路中心线。「道路中心线」工况下全部横向偏移固定为零，「极限工况」下各腿的横向偏移成为自由变量由求解器优化。它是纯几何约束，与载重、速度、路面无关。
_Avoid_: 行驶模式、驾驶策略

**档位**:
场景速算中区分 D 与 R 的量，其唯一几何含义是前后悬相对行进方向对调：行进方向前悬在 D 档取中心前距、R 档取中心后距。不含任何动力学。由此直角弯两条腿的宽度需求在两档之间整体对调。
_Avoid_: 前进后退、行驶方向

**双向**:
同一辆车在同一布局里向两个相反方向各做一次机动，判定取两个互为镜像机动中的最不利者。不是两辆车会车，也不建模错车间距。双向 U 型的中巷被两个机动共用，对称性把它的横向偏移钉死为零，因此极限工况下它要求的巷道宽不小于单向。
_Avoid_: 双车道、会车

**道路极限尺寸**:
场景速算正向计算的结果：帕累托前沿上的一个点，每一项尺寸在其余项取当前值时都已不能再单独缩小。有两项以上待求尺寸时解不唯一，收敛到哪一点取决于求解顺序。
_Avoid_: 最小道路尺寸、唯一解

**端墙**:
可通行区域多边形上纯为闭合而存在的边，位于道路继续延伸的方向。现实中那里没有墙，因此它们被外推到任何位姿都够不着，不参与净距测量；俯视图另按取景范围裁剪。
_Avoid_: 边界墙、区域边界

**下探长度**:
R 档直行转 D 档场景中，车辆倒车穿过路口后继续向下探入的深度，自转出道边界起算。
_Avoid_: 倒车距离

**出弯主路深度**:
R 档转弯转 D 档场景中，倒车摆出所占用的那一段主路，自支路开口的外侧壁沿主路量起。它由扫掠结果量得而非求解得出：主路两端都继续延伸、都不是墙，且双向布局里一条机动正是从另一条摆出的那一侧驶离，把它当成墙会挡住本该量的那次驶离。中心线工况下等于转弯半径加中心后距减支路半宽再加净距阈值。
_Avoid_: 支路深度、主路长度、路口尺寸

**端头深度**:
U 型转弯场景中，巷道尽头可供掉头的空间自隔墙顶端起算的深度。
_Avoid_: 尽端长度、掉头区深度
