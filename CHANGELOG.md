# 变更记录

## 未发布

### 新增

- 场景速算俯视图新增「模拟运行」：点一下让车体包络自起点沿路径走到终点，约 3.4 秒走完，双向布局的两个机动同时走。它只重画已经算好的位姿，不重新求解、不改任何尺寸；跑完停在终点而不是凭空消失，中途再点或改任何输入则立即中止。位姿展开此前每帧都要重算一次（含 shapely 包含判定），现改为随结果缓存一次，否则逐帧回放根本跑不动。
- 场景速算「直角 R 档转弯转 D 档」不再求解和显示**支路深度**，改为给出**出弯主路深度**。支路深度自机动改为「起点停在支路内」之后就不再构成约束 —— 支路是条继续延伸的路，车辆只是停在其中，它再深也不限制什么。新的出弯主路深度是倒车摆出所占用的那一段主路，自支路开口的外侧壁沿主路量起（同一组默认参数下 2.32 m）。
  它由扫掠结果量得而非求解得出：主路两端都继续延伸、都不是墙，而且双向布局里一条机动正是从另一条摆出的那一侧驶离，把它当成墙去二分会挡住本该量的那次驶离（实测另一条机动要驶到 7.31 m，墙会立在 3.57 m）。取值只在倒车位姿上量，最宽的一处正是车头与主路摆正的那一瞬；中心线工况下与闭式 `R + 中心后距 − 支路宽/2 + 净距阈值` 逐位相符。
- 场景速算的俯视图不再在路径旁标注「D档」「R档」胶囊。档位是左栏的输入，在图上重复一遍只是占地方。左栏的档位选择、复合场景的固定胶囊以及求解本身都不变 —— 已逐变体比对，全部可选档位变体解出的尺寸与改动前完全一致。路径仍以实线区分前进段、虚线区分倒车段，图例照旧说明其含义。
- 场景速算的分段选项改名为「整车 / 中心前距 / 中心后距」，与它正下方紧邻的两个尺寸输入框同名 —— 选项切的就是这两项，指着它们比描述它们清楚。地图页的同一分段仍叫「仅前段 / 仅后段」，那里没有相邻的尺寸输入可指。图例继续写作「扫掠 前段（中心前距）」，两种叫法都列出来，以便与地图页对上。
- 场景速算的「整车 / 仅前段 / 仅后段」由俯视图标题栏移到左栏车辆参数中，紧接中心前距与中心后距之下 —— 它切的正是这两项。原位置放不下：标题栏的控件按中文全角度量需约 830 px，而俯视图列宽只有 612 px，三个分段按钮的文字被省略号吞成「…」，标题也一并被截。移出后仍需 663 px，因此标题另行独占一行，图层开关与模拟运行留在下一行，控件行降至 302 px。
- 场景速算的扫掠包络新增「整车 / 仅前段 / 仅后段」分段，与地图页车辆图层同一套语义：分界落在前轴中心点上，仅前段画出的正是中心前距那一段。前段与后段各有一个固定颜色，两者的对应关系写在图例里（「扫掠 前段（中心前距）」「扫掠 后段（中心后距）」），否则固定配色没人看得懂。分段只影响绘制，不触发重算，也不改变净距与求解结果。
- 场景速算俯视图的图例改为放不下时换行，不再走完一行就停下、把排在后面的条目悄悄丢掉。加入行进方向、起点、终点与两段扫掠颜色后条目增至 11 条，按中文全角度量约需 940 px，而俯视图列宽只有 640 px 上下；原来的单行图例会直接截掉尾部，而两段固定色一旦没有旁边的文字就完全无从分辨。
- 修复双向 U 型转弯的起点与终点弄反：两条机动此前都自**外巷**起步、驶回共用的**中巷**，等于让两条机动在同一条巷道里迎头相对。现改为都自中巷起步、各自驶入一条外巷。
  用的是同一个半圆，只是方向相反，但结果确实会变：D 档下原方向把后外角甩向外侧墙（余 1.5 m），新方向把它甩向敞开的端头（余 5.5 m），因此双向反而比单向宽松（中心线工况实测单向 0.447 m 对双向 0.537 m）。[ADR 0008](docs/adr/0008-report-scenario-limits-as-a-pareto-point.md) 中“极限工况下双向要求的巷道宽不小于单向、中心线工况两者相同”那条断言随之撤销 —— 它的依据是两者机动形状相同、仅偏移受限，前提已不成立。改为钉住两条确实成立的性质：两条机动互为镜像因而同样紧，以及中巷偏移固定为零。
  U 型的 72 条原型对拍夹具用例随之失效（原型实现的是旧方向），与此前借支路场景的 36 条一并排除。逐位对拍现由直角转弯与 R 档直行转 D 档的 108 条承担 —— 夹具已有一半不再适用，它现在证明的是区域构造、采样与镜像与一份独立实现一致，但覆盖的形状比以前少。
- 场景速算俯视图的起点、终点不再在图上标文字，只留圆点：标注贴在几像素宽的圆点旁，盖住的正是它要指的那个点。哪个点是哪个改由图例说明。
- 与之配套修掉两处会让无文字圆点失效的问题：终点原本与最小净距点共用同一种红，去掉文字后两者成了无从分辨的同色圆点，现给终点单独一种颜色；圆点原本在尺寸标注之前绘制，而标注文字的底衬不透明，极限工况下「出弯主路深度」的标注整块抹掉了起点圆点（实测该处像素读到的是标注底衬色而非圆点色），现改为圆点最后绘制。新增按渲染后像素取色的测试，覆盖全部场景 × 工况，这类检查不受机器有无中文字体影响。
- 场景速算的路径新增行进方向箭头与起点、终点标记。箭头按**行进方向**而不是车头朝向绘制 —— 倒车段两者相反，而路径要表达的正是车体往哪边走。
- 新增“场景速算”标签页（画布区第三个标签，位于“地图”“通行余量”之后）：不连接任何数据源，仅凭车辆参数现场速算八种典型路口/掉头工况的道路极限尺寸。覆盖直角转弯、R 档直行转 D 档、R 档转弯转 D 档、U 型转弯四个场景 × 单/双向 × 档位 × 中心线/极限工况共 24 个变体，每个变体可正向求道路极限尺寸，也可反向校核给定尺寸并判定通过/临界/越界。判定规则与既有越界分析同源。整页离线，主窗口没有选中命令时也可用；车辆参数默认取 `config.json`，页内改动只留在内存，不写回配置、不写 `data/`。
- 正向计算给出的是帕累托前沿上的一个点 —— 每一项尺寸在其余项取当前值时都已不能再单独缩小 —— 而非唯一解，界面据此命名为“一组最小可行尺寸（非唯一）”并写明改变求解顺序会得到另一组解。逐项压缩改用“先粗扫定位最低可行样本、再在其左邻域二分”，因为可行性并不随尺寸单调：U 型巷道加宽会同时放大掉头半径、使扫掠顶点更深地探进端头。决策记录见 [ADR 0008](docs/adr/0008-report-scenario-limits-as-a-pareto-point.md)。
- “双向”定义为同一辆车向两个相反方向各做一次机动、取两个镜像机动中的最不利者，不是两辆车会车。双向 U 型的中巷被两个机动共用，对称性把它的横向偏移钉死为零，因此极限工况下它要求的巷道宽不小于单向；中心线工况两者相同。
- 可通行区域多边形上纯为闭合而存在的端墙被外推到任何位姿都够不着，净距只由真墙决定。端墙参与净距会把最小净距钉死在路径缩进量上、并让净距阈值一旦大于该值就使所有场景永久不可行。
- 净距引擎整批位姿进 numpy 数组后走 shapely 向量化接口，最重的一次正向求解（双向 + 复合场景 + 极限工况）约 1.2 s，跑在 `QThreadPool` 上并按代次计数丢弃过期结果；任何输入或分段切换后 150 ms 防抖重算，重算期间控件不禁用。图层开关只重绘不重算。
- 俯视图由 QPainter 自绘：0.5 m 网格、可通行区域、道路中心线、车辆扫掠范围、D/R 档路径、越界位姿、尺寸标注、档位胶囊、U 型起弯点与最小净距点。右栏给出判定、道路尺寸、校核明细、各腿最优偏移与可行偏移区间、四角转弯半径（复用既有闭式公式）和一句话瓶颈说明。
- 场景速算只算不改：不连接调度后端、无网络请求、无持久化，结果不落盘。U 型巷道宽加隔墙宽装不下最小转弯半径时不画那条画不出来的弧，改为直接给出至少需要多宽；正向无解时给出阈值需降到多少以下才有解。
- `numpy` 由 shapely 的传递依赖提升为显式声明依赖。
- 新增“通行余量”分析：画布区分为“地图”和“通行余量”两个标签页。通行余量针对当前命令的下发路径，把净距剖面与逐段可行偏置带画在同一条横轴上，给出瓶颈排行榜、内嵌的需求道宽三区标尺和三张优化建议卡。偏置定义为把一个区段的全部位姿沿行进方向的法向整体平移，正值指向左侧；可行带由粗扫加二分求出使该段最小净距等于阈值的边界。相邻两个转弯之间的连接段只有一个偏置值，因此该段区间取前后两个转弯要求的交集，交集为空时报告耦合冲突并给出差值。选中瓶颈行只定位不切页（该行的产出是它下方展开的三区标尺），点击主图才切到“地图”页定位。
- 需求道宽通过改写覆盖车道的总宽、重建通行区域后重跑既有的扫掠判定链路二分求得，不使用闭式公式；给出“居中即可通过”和“需路径偏置”两个下界。
- 新增“转角求解”二级视图：选中瓶颈榜中的转弯行后点标题栏的“进入转角求解”按钮进入，提供入弯段偏置、出弯段偏置和起弯点三个自由度。两条腿的偏置定下后，切于两腿且转角固定的圆弧只剩一个参数族，起弯点选的正是族中的哪一条，因此路径转弯半径是三个自由度的因变量 —— 推迟起弯收紧半径，提前起弯放大半径而扫掠带反而变窄。视图分别按两个和三个自由度各求一次，给出第三个自由度带来的增益。
- 车道倒角半径与路径转弯半径不等时给出偏置沿弯变化曲线：横向间距沿弯从零变到峰值再回到零，峰值为 `|R_lane − R_path| · (1/cos(Δ/2) − 1)`，两条弧的切点在直线上相距 `|R_lane − R_path| · tan(Δ/2)`；曲线标出常数偏置近似恰在弯心处高估的余量。俯视图把车道走向线与下发路径分开画。
- 通行余量可导出偏置表 CSV（UTF-8 带 BOM）与 A4 横向 PDF 报告。报告的概要卡、建议卡和全部图表直接复用界面上的同一套控件与绘制代码，纸面与屏幕同源且文字为矢量；页眉带齐报告号、日期、阈值、订单、任务、命令、车型尺寸及其来源、车道布局、步长与样本数，页脚为免责声明。报告内容按所见即所得确定，在转角求解页导出会追加该转角一页。
- 通行余量只给建议，不写回下发路径也不修改车道；所有建议卡带“只建议 · 不修改”标签，不提供一键应用。全部结果为当次会话的派生结果，不落盘，CSV 与 PDF 是仅有的出口。决策记录见 [ADR 0007](docs/adr/0007-solve-clearance-headroom-as-path-offset.md)。
- `scripts/render_release_preview.py` 增加一张通行余量页的离屏预览，使这一整页 UI 进入发布前的视觉检查。
- 新增 Linux 脚本 `scripts/bootstrap.sh`、`scripts/run.sh` 和 `scripts/build.sh`：环境准备按 `python3.12`、`python3.13`、`python3.14`、`python3` 顺序选取第一个满足 `>=3.12,<3.15` 的解释器并打印实际版本；启动脚本转发全部命令行参数，并在既无显示会话又未指定 `QT_QPA_PLATFORM` 时提前报错；构建脚本在既有质量门禁之外增加架构与 glibc 基线检查、ELF 产物校验、离屏与真实平台两次打包烟雾，并输出产物文件数、总字节数和入口可执行文件 SHA-256。
- Linux x64（x86_64 加 glibc 2.43 基线）成为第二个受支持发布平台，与 Windows x64 共用 `route-analysis.spec` 及文件夹模式发布物；决策记录见 [ADR 0006](docs/adr/0006-support-linux-as-a-second-release-platform.md)。

### 变更

- 场景速算左栏的「计算方向：求道路极限 / 校核给定道路」与「工况：道路中心线 / 极限工况」两组控件合并为一组**计算方向：道路中心线 / 帕累托极限**。可偏离中心线与可固定参数总是一起出现，校核只是把参数全部固定的特例，两组控件等于给同一件事开了两个入口。道路中心线下全部横向偏移为零、道路尺寸全部由求解器给出，左栏只列给定项（U 型隔墙宽，后缀「（给定）」）；帕累托极限下左栏列出该场景全部道路尺寸与全部腿的路径偏移（含 U 型起弯点），**改过哪一项哪一项就固定**：行右侧的「固定」开关自动勾上、求解器不再动它，其余尺寸压到极限、其余偏移重新优化并回填到输入框，取消勾选即交回求解器。双向布局里被两条镜像机动共用的腿（T 口支路、十字倒车道、借支路的支路、三巷的中巷）偏移恒为 0，该行置灰、不可固定；偏移允许填超出可行区间的值，结果直接报越界而不拦截。切换场景、单/双向、档位或计算方向时释放全部固定（布局一变，固定的数就不再对应同一条腿），改车辆参数保留。
  有横向偏移被固定时不再以中心线解封顶：封顶的依据是「车辆可以停回中心线，中心线解必然可行」，偏移一旦被用户钉住这条依据就没了，被固定的偏移可能真的要求比中心线更宽的路，把它压回中心线解等于否掉用户的输入。未固定横向偏移时封顶照旧，帕累托极限的每一项尺寸仍不大于同参数下的中心线解。左栏偏移行按腿的角色命名：直角转弯单向叫「进入道偏移 / 驶出道偏移」，双向 T 口改叫「主路偏移 / 支路偏移」—— 双向没有固定的进出方向，两条机动各进各的；借支路为「主路偏移 / 支路偏移」，十字为「倒车道偏移 / 转出道偏移」，U 型双向为「中巷偏移 / 外侧巷道偏移」。
- 重做原型对拍夹具并同步 `.dc.html`：把借支路与双向 U 型两处机动的改动补进原型的几何层，重新生成 216 条用例，Python 侧逐位相符，此前因需求变更排除的 108 条全部恢复对拍。原型 UI 侧 stubback 的求解量与标签一并去掉支路深度，否则页面会去解一个几何层已经不看的尺寸。
  **恢复全量不等于恢复全部效力，这一点写进了测试**：原型本是设计交付稿先写、Python 由它移植而来，一致证明的是移植忠实；这两处机动如今是照产品改的原型，等于同一份理解写了两遍，机动形状不再是独立佐证。仍然独立的是求值链路 —— 原型自己算线段间距与点在多边形内，Python 走 shapely 向量化接口，区域构造、位姿采样、镜像与净距计算对四个场景依旧是真正的交叉验证。原型的**求解层**未同步，自「同步收缩」与「中心线封顶」两次改动后其极限尺寸数值已不代表产品行为。
- 补齐文档对齐：README 此前仍写着「画布区分『地图』和『通行余量』两个标签页」（实为三个）且完全没有场景速算；用户手册缺场景速算一节；架构说明的模块清单缺 15 个模块 —— 整个 `clearance_*` 与 `scenario_*` 家族，前者的缺口早于本轮工作；实施清单停在 0.4.0。四处一并补上，并把场景名、默认值、配色、回放时长等事实性断言逐条对代码核实过。
- 净距阈值默认值由 `0.05 m` 改为 `0.15 m`。这是全局默认值，通行分析的绿/黄/红判定、通行余量求解和场景速算共用它；场景速算继续从 `config.json` 的 `analysis.clearance_threshold` 取值，两层默认值保持一致。**已有 `data/config.json` 中写死的旧值不受影响**，只有全新安装或配置里缺该字段时才取新默认值；要在现有机器上生效需到设置里手动改。
- 场景速算的转弯半径默认值由 `1.60 m` 改为 `1.20 m`。该值仍是经验值而非车型标称值，左栏脚注同步改写。八个场景在新默认参数（R 1.20 m、阈值 0.15 m）下全部有解。

- 「按路径生成」改为在当前路径上选两个点位生成车道：点击后画布进入选点模式，逐个确认两个点位，复用手动半径测量那套端点候选列表（序号从 1 开始、`Esc` 退出）。两种选点模式互斥，进入其中一个自动退出另一个。对话框不再提供来源下拉，来源由当前点位标签对应的路径确定。
- 生成的车道两端各按该点位的**车体外伸量**自动延伸，使两个点位的车辆包络在长度方向上都落在车道内。外伸量取车辆包络四角在该端走向线方向上的最大投影：走向线方向与位姿航向一致时它就是中心前距或中心后距，倒车段或直线连接两个朝向不同的位姿时仍然正确。
- 新增「连接方式」：沿原路径保留两点之间的实际形状，直线连接忽略中间形状。直线连接下弯道生成模式、最大拟合偏差和闭合车道禁用并说明原因，不静默忽略。上次选择记入 `config.json` 的 `lane_connection`。
- 车道总宽小于车宽时对话框提示两端车体会露出车道，但不自动改宽度也不拦截生成。
- 「车辆」显示开关由复选框改为四态：整车 / 仅前段 / 仅后段 / 关，下发与实际各自独立。分界落在前轴中心点上，仅前段画出的正是中心前距那一段。这只影响绘制，越界判定、最小净距和通行余量分析永远使用完整车辆包络，通行余量视图与导出报告中的车辆图形也不随之改变。

### 移除

- 移除场景速算的「校核给定道路」计算方向与右栏「校核明细」卡。在帕累托极限下把全部尺寸固定即为校核，判定、最小净距、相对阈值余量与瓶颈并入求解状态卡；单独保留一个校核入口只是同一件事的第二个名字。
- 移除「整条路径一次生成车道」：生成范围一律由两个点位界定。需要整条时选首尾两个样本、连接方式用「沿原路径」即可，结果与此前一致。

- 移除“自动计算半径”：系统不再识别转弯区间、不再批量生成自动半径测量，全部转弯半径测量都由用户逐条选点建立。读取本地测量文件时丢弃早期版本写入的自动记录，只保留用户选点建立的测量，文件不再写入自动测量的计数字段。
- 移除“转弯识别阈值”设置项：删除自动识别后它不再影响任何行为，继续提供会是一个转不动任何东西的开关。本地 `config.json` 中早期版本写入的该字段被忽略。`turn_radius.py` 中的转弯区间识别纯函数及其测试予以保留。

### 修复

- 直角 R 档转弯转 D 档的俯视图不再把起点圆点、「支路宽」标注与最小净距点挤在支路口：起点改停在支路更深处、先沿支路倒车直行一段再摆出（直行段每个位姿的横向几何与摆出起点相同，净距与出弯主路深度逐位不变），「支路宽」标注移到支路画出的下端。原型几何层同步，216 条对拍夹具重新生成。

- 修复直角转弯的机动方向弄反：此前自主路驶入支路，双向 T 口的两条镜像机动因此在共用的支路里迎头相对 —— 与双向 U 型此前犯的是同一个错。现改为单双向都**自支路起步、驶出到主路**（双向各自驶出到西、东两侧）。单向的「进入道」随之改指支路、「驶出道」改指主路，偏移行同步换名；同一条弧反着走等于前后悬对调，D/R 两档的数值互换（如中心线 D 档由 3.64/2.67 变为 2.67/3.64）。原型几何层已同步并重新生成 216 条对拍夹具，直角转弯的机动形状自此与借支路、双向 U 型一样不再是独立佐证。

- 修复场景速算「直角 R 档转弯转 D 档」的支路偏移 `so` 符号与「弯道内侧为正」约定相反：此前正值把路径推向弯道外侧，与其他腿的偏移读法相反，在左栏里把它固定成一个数时会走到反方向。几何层改为内侧为正（`_stubback` 不再对 `so` 取反）。原型对拍夹具 `tests/fixtures/scenario_clearance.json` 由旧符号的原型生成，夹具本身保留不改，加载时把 `so` 取反后再喂给 Python，以保持逐位对拍。
- 修复场景速算 U 型转弯在只固定巷道宽、隔墙宽与转弯半径装不下半圆时，报的是泛泛的「找不到可行尺寸」而不是「巷道至少需 X m」：巷道间距一旦被固定，半圆装不装得下不需要搜索就能判定，现在先判定再搜索端头深度。
- 修复场景速算偏移优化在「车体已越线但位姿中心仍在区域内」处读到平坦的 0.0 而原地不动：此前中心线封顶保证搜索起点可行，问题不暴露；固定尺寸或偏移后封顶取消、起点可能落在这块高原上，报出的极限尺寸偏大（直角固定进入道宽 3.0 m 时驶出道宽报 2.386 m，实际约 2.15 m），全部固定的校核会把可行道路判成越界。搜索路径现在对越线位姿给出角点穿透深度作坡度（四角都在内而凹角顶入车体时退回精确穿透）；汇报路径的偏移下降改为先在粗采样下从居中起点与粗网格最优角各下降一次、取更优者，再在细采样下做两轮收窄的轴向抛光与一次沿每对偏移对角线的扫描 —— 轴向逐坐标扫描会在两条腿互相牵制的斜脊上停步几厘米，足以让求解报「已求解」而对同一组数的全固定校核报「临界」。一次求解内同一道路的汇报结果复用。最重的双向 U 型帕累托求解约 5 s（此前约 2.4 s），重算期间标题栏显示「计算中…」。
- 修复 U 型只固定巷道宽时的「装不下转弯半径」判定按偏移为 0 计算：帕累托极限下自由的巷道偏移可以贴向外侧墙把半圆撑大，此前把可解输入判成不可建（车 1.0/1.0/0.5 m、R 1.5 m、巷道 2.5 m + 隔墙 0.3 m 实为可解）。现在按偏移贴外的最大转弯半径判定，所需巷道宽也按此二分求得。
- 修复场景速算求解在途时改值并自动固定后、150 ms 内返回的旧结果把求解值写回刚固定的输入框：任何输入变化现在都推进代次，在途结果一律丢弃；回填也跳过当前已固定与正在键入的行。点击已选中的计算方向/场景/单双向/档位按钮不再释放固定并重解。通过「固定」开关固定时冻结的是屏幕上正显示的值，而不是内存里可能过期的值。
- 修复帕累托极限下侧栏内容比视口宽约 35 px、固定按钮被裁：道路行的输入框改为可压缩（保底 72 px），共用腿改用「共用」标记取代固定按钮，行标题不再加后缀。
- 修复双向 U 型固定巷道宽时求解报「已求解」却低于阈值的两条路径：帕累托侧的偏移优化候选起点此前不含中心线道路自己的最优起弯点，而该起弯点的可行口袋比粗网格步长还窄，现把中心线最优偏移也作为候选；中心线封顶道路本身也可能因收尾整体加量三轮闭合不了粗筛的高估而停在阈值以下，加量改为按缺口的两倍、最多六轮。状态卡的「已求解」只在结果确实达到阈值时显示，否则显示判定词并提示放宽固定或阈值。
- 场景速算侧栏的输入框只在获得焦点后才响应滚轮：滚动侧栏时指针经过的输入框此前会被滚轮改值，现在改值即固定，这会在无意中固定一堆行。取消固定时丢弃框内尚未提交的文本，否则焦点一离开它又会自动固定；结果栏滚动区保留焦点以便键盘滚动，侧栏滚动区不取焦点（否则点「固定」会先夺走输入框焦点、提交文本并自动固定，再被同一次点击的切换释放）。
- 修复通行余量三区标尺的标题把净距阈值写死成 `0.05 m`：它显示的是当时的默认值而不是本次分析真正用的阈值，任何改过阈值的人看到的都是错的。`WidthZones` 现在带上求解所用的阈值，标题据此渲染。
- 修复场景速算「直角 R 档转弯转 D 档」的机动与需求方原始草图对不上。此前按设计交付稿的「借支路掉头」建模：自西驶来 → 倒车弧切入支路 → 立刻另一条弧驶出 → 向西离开。两条弧同切支路中线，前轴中心最深只到主路中线以下一个转弯半径（实测 y=−1.20，而主路南墙在 −1.42），**车辆的参考点自始至终没有离开主路**，支路深度纯粹是为了容纳车尾摆动。该场景在设计交付稿里本就列为待需求方确认项。现按需求方确认的语义重建：**起点整车停在支路内、车头朝南，R 档一段四分之一弧倒转至主路东侧，车头朝西摆正后 D 档向西直行**，机动由四段缩为两段。同一组车辆参数下主路宽 2.84 → 3.67 m（车尾摆向北墙）、支路宽 3.63 → 2.50 m（车尾不再摆进支路）、支路深度 2.16 → 1.06 m（只需容纳车头）。场景副标题相应由「借支路掉头」改为「自支路倒车汇入」。
  该场景的 36 条原型对拍夹具用例随之失效 —— 夹具取自设计稿原型，而原型实现的是旧机动，比对它只会把旧形状钉回去。这 36 条已从逐位对拍中排除并在测试里写明原因，其余 180 条继续对拍；夹具文件本身保留为原型读数的存档。
- 修复场景速算「极限工况」解出的尺寸反而大于「道路中心线」工况：极限工况只是多给自由度（各腿横向偏移都可以取零），中心线解在极限工况下必然可行，因此没有任何一项尺寸应该变大，否则等于告诉使用者「允许车辆偏离中心线反而要求更宽的路」。十二个正向变体里有十次这样，双向「直角 R 档直行转 D 档」最夸张 —— **转出道宽 4.46 m，而同参数中心线只要 2.51 m**。贪心的这次是偏移：下降早期道路还宽，大偏移不花代价却能换来足够净距让其余尺寸猛缩，那个必须容纳该偏移的尺寸随后再也降不回来。实现改为先解中心线变体、再用它逐项封住极限变体的搜索上界。该条作为不变量固定在测试中，四组参数（R 1.2/1.6/2.5 × 阈值 0.05/0.15/0.3）下全部成立。
  封顶会拿掉一些真实解：把转出道宽与下探长度按住在中心线取值后，单向倒车道宽也降不到更小了，而不封顶时它本可以用深 10 cm 的下探换一条更窄的倒车道。这个交易不再提供 —— 极限工况现在表述为「在中心线方案之上偏移还能省多少」，而不是另换一条道路。
- 修复场景速算正向求解收敛到帕累托前沿的极端顶点：待求尺寸此前逐项压缩，排在第一位的那项是在其余尺寸仍停在搜索上界、因而什么也约束不住的环境里被最小化的，于是它吃掉全部预算、其余尺寸再也动不了。双向「直角 R 档直行转 D 档」因此把**转出道宽解到 7.02 m** 配 0.36 m 下探长度，越过了它自己 6.93 m 的搜索上界（靠收尾的整体加量才落回可行），而界面照样报“已求解”；单向同一组车辆参数给出的 2.31 m 配 2.72 m 放进双向布局里同样成立。改求解顺序只是把爆掉的那一项换成新排在第一位的（先压转出道宽会得到 6.12 m 的倒车道宽），六种顺序无一能找到平衡解。改为让全部待求尺寸以同一步长一起收缩、直到每一项各自顶到自己的墙为止，耦合在整个下降过程中始终可见。双向现在与单向给出同一组尺寸（3.18 / 2.31 / 2.72 m）。
- 同步收缩一并修正了另外两处：双向 U 型极限工况此前解出的巷道宽 2.25 m 小于单向的 2.54 m，与 [ADR 0008](docs/adr/0008-report-scenario-limits-as-a-pareto-point.md) 写明的“极限工况下双向要求的巷道宽不小于单向”相矛盾，现为 2.80 m 对 2.63 m；双向「直角 R 档转弯转 D 档」的转出道宽由 5.24 m 降到 4.03 m。八个场景的正向求解现已无一项停在搜索上界，该条作为不变量固定在测试中。
- 场景速算正向求解同时变快：可行性判定次数约降到原先的四分之一，最重的一次（双向 + 复合场景 + 极限工况）在同一台机器上由约 1.9 s 降到约 1.3 s。
- 修复 `scripts/render_release_preview.py` 无法运行：移除自动半径测量时一并删掉了 `RadiusMeasurementState.replace_automatic`，但脚本仍在调用它，且该脚本不在测试覆盖内因此未被发现。改用 `add_manual`。

### 变更

- 手动选点模式不再预先高亮系统建议的转弯端点：进入模式时画布上不添加任何标记，只在用户选定端点后才出现标记。
- 选点模式下每次在路径上点击都弹出端点候选列表，由用户从列表中确认端点；此前只在命中多个重合样本时才弹出，命中单个样本直接选中。列表内容为离点击位置最近的样本、它前后各两个可绘制样本，以及本次点击命中的其余样本，合并去重后按路径顺序排列，最近的一项加粗；路径首尾附近邻居不足时相应变短，不向另一侧补齐。关闭列表而不选择时不改变当前选择。
- 画布只对已确认的转弯端点标红圈并标注序号，候选中的其余样本保持普通路径点外观。已选定的入弯端点标记保持到开始选择下一条测量或退出选点模式才清除，因此选定出弯端点后两个端点同时标红。
- 端点候选列表和画布端点标注统一使用与左侧点位表格一致的、从 1 开始的样本序号；候选列表此前显示的是从 0 开始的内部序号，与点位表格差一。状态栏的入弯样本提示同步改用同一序号。
- 转弯半径结果列表不再分“自动测量”“手动测量”两组，改为按创建顺序平铺展示；按钮文案由“自动计算半径”“手动计算半径”改为“计算转弯半径”，进行中文案为“结束选择端点”。新建测量的默认名称仍为“手动半径 N”，已保存测量的名称不被改写。
- 路径中心线图层在折线之外为每个具有有效坐标的位姿点标出一个圆点，任何缩放下都不按密度丢弃标记，画布上的圆点数量恒等于该路径可绘制的位姿点数量。圆点直径以屏幕像素计量：稀疏处取基准直径 7 px，拥挤处按相邻两点中较小的屏幕间距成比例收缩至最小 1.4 px，因此密集的实际执行路径缩小时圆点变小而不会连成实心带，也不会盖住另一条路径，放大后重新展开为可分辨的标记。中心线折线相应由 2.2 px 减细到 1.2 px。
- 缺少 yaw 的位姿点在中心线图层中绘制为空心圆点，关闭车辆矩形图层时也能看出哪些点被排除在转弯和越界分析之外；空心圆点使用与普通圆点相同的收缩规则，密集处需要放大才能分辨。非当前点位标签对应路径的圆点绘制为更小的半透明空心圆，与“非当前标签对应路径不参与命中”的既有规则一致。
- `scripts/render_release_preview.py` 改为按平台选择中文字体，不再硬编码 Windows 字体文件路径。
- README、用户手册、规格说明和威胁模型同步两个平台的准备命令、发布路径与访问控制缓解措施。

### 修复

- 修复车道锚点或贝塞尔控制点压住路径点时该点无法点选：点击命中控制点会直接丢弃路径点候选，而自动生成的车道锚点正落在来源路径坐标上，导致大量路径点点不中。现在控制点上按下后未拖动即释放时会选中同位置的路径点，发生拖动时仍然只移动控制点。
- 修复坐标输入框在字体宽于 Windows 基线时裁切内容：Linux 上“地图方向”显示为 `0.000000 rac`，`rad` 的结尾被截断，微调按钮同时被挤压。输入框的宽度上限改为按当前字体下实际显示的文本计算，并在字体或样式变化时重算；上限不低于原有 115 px，因此在放得下的字体度量下布局保持不变。

## 0.5.0 - 2026-08-16

### 新增

- 手绘车道首次固定锚点后，绿色虚线中心线和半透明车道总宽区域实时跟随鼠标；候选端点与下一次点击使用同一套路径点吸附结果。
- 首次点击前显示候选锚点；绘制中支持 `Backspace` 撤销最后一个锚点，并在状态栏提示固定、重合、点数不足、取消和完成状态。

### 变更

- 手绘车道的总宽输入在绘制过程中实时更新预览；至少固定两个不同锚点后，才可通过按钮、`Enter` 或双击固化到当前编辑文档。
- 鼠标移出画布时隐藏动态候选点和末段，保留已经固定的草稿几何；返回画布后继续预览。

## 0.4.0 - 2026-08-16

### 新增

- 在左侧命令表下方新增可拖动点位详情，分别列出下发和实际路径的序号、X、Y、Yaw（rad）及 Gear，并支持按序号跳转。
- 新增完整命令 JSON 与选中点位 JSON 只读视图，保留后端全部原始字段和异常点位内容。
- 新增点位表格与地图双向选择；选中时以独立图层高亮前轴中心和车辆框，视口外自动居中，重合样本可连续点击循环选择。
- 新增下发/实际路径独立加载状态、单来源错误提示与重试；一侧失败不影响另一侧已经加载的数据。

### 变更

- API 路径结果改为同时提供完整原始命令/点位数据和有效几何位姿投影；无效单点不再阻断整条命令的 JSON 检查。

## 0.3.2 - 2026-08-14

- 修复从命令返回任务列表后，画布仍显示原命令路径但通行分析因导航命令上下文已清除而保持“不可分析”的问题；当前显示路径现在保留独立分析上下文，车道编辑和“立即重新分析”均可继续生成有效样本。

## 0.3.1 - 2026-08-14

- 修复修改全局或 VIN 专属车辆尺寸后，四角半径和通行分析已使用新参数，但路径车辆框及半径定位端点车辆框仍沿用旧尺寸的问题；保存设置后画布车辆包络现在立即同步重绘。

## 0.3.0 - 2026-08-14

- 将局部距离窗口半径统计替换为整弯端点测量：每组入弯/出弯样本计算一个等效旋转中心，以及前轴中心、前外角、后外角、前内角、后内角五个固定半径。
- 新增下发/实际路径各自的后台自动计算和连续手动选点；支持建议端点高亮、重叠样本选择、可编辑唯一名称、单条删除及自动组原子替换。
- 新增转弯半径测量本地持久化，按后端、租户、订单、任务、命令和路径类型隔离，并以完整有序 `(x, y, yaw)` 指纹自动失效旧结果。
- 新增整弯定位图层：高亮测量区间、两端车辆、等效圆心、五条半径线和五条完整圆弧轨迹。
- 车道属性新增真实中心线总长；支持按包围盒中心统一缩放直线、真实圆弧和三次贝塞尔几何。
- 新增整车道鼠标拖动和路径点吸附，禁用车道仍可编辑；一次连续拖动只生成一个撤销步骤。
- 移除当前配置和设置界面的旧半径距离窗口，读取旧配置时忽略遗留字段。

## 0.2.1 - 2026-08-14

- 修复“按路径生成”参数窗口被错误转为普通子控件而不可见的问题；窗口现在会以前置模态对话框显示，确认后可正常新增并保存车道。

## 0.2.0 - 2026-08-14

- 新增按下发或实际路径自动生成独立车道，支持尖角、真实相切圆弧和切向连续的分段三次贝塞尔。
- 新增半透明生成预览、最大拟合偏差、近闭合检测、圆弧失败回退、仅新增和单次撤销。
- 车道 schema 升级为 2，写入真实圆弧圆心与方向；schema 1 继续可读并在手动保存时备份升级。
- 新增圆弧半径查看和编辑，保持相邻直线方向与理论交点，超出最大允许半径时拒绝。
- 新增基于路径 `(x, y, yaw)` 的转弯/掉头识别、距离窗口瞬时旋转中心和车辆四角半径统计。
- 新增每弯道及全局的前外、后外、前内、后内最大半径与位置，详情显示最小值、中位数、最大值。
- 新增默认关闭的转弯半径画布图层，可定位 ICR、车辆矩形、四条半径线、局部圆弧与标签。
- 新增同级 `log/` 结构化日志、INFO/DEBUG 等级、每日或 20 MiB 混合轮转、30 个历史文件保留和故障自动恢复。
- DEBUG 按明确决策记录未脱敏凭据、请求头、配置、请求/响应、完整路径和导入正文；帮助菜单可打开日志目录或当前日志。
- 修复密集路径圆弧拟合的三次方性能问题；400 点半圆基准由约 6163 ms 降至约 8 ms。

## 0.1.0 - 2026-08-13

- 新增只读调度认证、订单 ID 查询、服务端分页和订单→任务→命令导航。
- 新增下发/实际路径同画布显示和独立图层控制。
- 新增以前轴中心、车宽、中心前/后距和 yaw 生成车辆矩形。
- 新增弧度地图方向显示变换和原始坐标编辑。
- 新增直线/三次贝塞尔车道、开放/闭合、每车道宽度、圆角/尖角及锚点覆盖。
- 新增多车道并集、连续插值扫掠和绿/黄/红净距结果。
- 新增缺 yaw 不推断规则、VIN 车辆尺寸覆盖、撤销/重做、手动保存和脏状态提示。
- 新增按服务器/mapId 的本地车道、前版备份、JSON 导入仅替换和导出。
- 新增 PyInstaller Windows x64 文件夹构建与离线冒烟验证。
